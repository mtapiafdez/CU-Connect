<%- include("../includes/head.ejs") %>
</head>

<body>
    <%- include("../includes/navigation.ejs") %>

    <main class="container">
        <h1>Home</h1>
        <% if (userType === "alumni") { %>
            <h2>Alumni Loaded</h2>
        <% } else if (userType === "admin") { %>
            <h2>Admin Loaded</h2>
        <% } else if (userType === "student") { %>
            <h2>Student Loaded</h2>
        <% } else { %>
            <h2>Public Loaded</h2>
        <% } %>
    </main>
<%- include('../includes/end.ejs') %>